{
  "name": "substrate-kernel-download-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that proxies the substrate-kernels R2 bucket and emits one proxy_download analytics event per full download. Self-contained: the analytics producer is implemented locally per the analytics queue protocol spec (analytics ADR 0015's hand-roll path).",
  "scripts": {
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.16.10",
    "@cloudflare/workers-types": "4.20260527.1",
    "typescript": "5.9.3",
    "vitest": "4.1.7",
    "wrangler": "4.95.0"
  }
}
